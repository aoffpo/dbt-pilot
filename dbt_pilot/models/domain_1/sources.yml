version: 2

sources:
  - name: pilot_db
    schema: public
    database: pilot_db
    tables:
      - name: asset # NOTE: docs are generated with columns in postgres, but not redshift or snowflake
      - name: user
      - name: location

